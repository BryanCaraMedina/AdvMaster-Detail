<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdvMasterDetail"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".register.RegisterActivity" android:exported="false"/>
        <activity android:name=".movies.MovieListActivity" android:exported="false" android:label="Películas"/>
        <activity android:name=".moviedetail.MovieDetailActivity" android:exported="false" android:label="Detalle de Película"/>
        <activity android:name=".series.SerieListActivity" android:exported="false" android:label="Series"/>
        <activity android:name=".seriedetail.SerieDetailActivity" android:exported="false" android:label="Detalle de Serie"/>

    </application>

</manifest>